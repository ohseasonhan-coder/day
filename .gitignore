node_modules/
build/
release/
.env
.env.local
.DS_Store
*.log

# 개인정보/원본 데이터 — Git 커밋 금지 (로컬 전용)
# 피드백/검수 원본 엑셀(원아 이름·관찰 원문 포함 가능)
쌤워크_피드백_서식*.xlsx
*_피드백_서식*.xlsx
# 로컬 골든 데이터셋(비식별 전 원본 기반) — 추적 제외
data/golden_local/
*.local.json
*.local.md

# 로컬 LLM 모델 가중치 — Git/번들 커밋 금지(런타임에 기기 캐시로만 저장)
*.gguf
*.onnx
*.safetensors
*.wasm.bin
models/
model_cache/
